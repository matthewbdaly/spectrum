// @flow
import React, { Component } from 'react';
import './Loader.scss';

type Props = {
};

class Loader extends Component<Props> {
  render() {
    return (
      <div>
      </div>
    );
  }
}

export default Loader;
